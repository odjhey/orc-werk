---
id: ADAPTER-GIT-MAPPING
type: adapter-mapping
status: draft
authority: informative
description: Draft Git-to-CandidatePort mapping.
---

# Git candidate mapping

The first implementation should define exactly which Git identities contribute to Candidate equality/freshness and encode that descriptor deterministically.

It must satisfy `INV-006` and `INV-010` and conformance `CONF-CAND-001` through `CONF-CAND-003`.
